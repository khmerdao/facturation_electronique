{% extends 'base.html.twig' %}
{% block page_title %}Nouvelle facture{% endblock %}

{% block main %}

<div class="d-flex align-items-center gap-3 mb-4">
    <a href="{{ path('app_invoices_index') }}" class="btn btn-sm btn-outline-secondary">← Retour</a>
    <h1 class="h4 mb-0 fw-bold">Nouvelle facture</h1>
</div>

{{ include('invoices/_form.html.twig', {
    form_action:   path('app_invoices_new'),
    csrf_token_id: 'create_invoice',
    submit_label:  'Enregistrer le brouillon',
    is_edit:       false,
    back_url:      path('app_invoices_index'),
    cancel_url:    null,
    invoice:       null,
    contacts:      contacts,
    formats:       formats,
    products:      products,
    sequences:     sequences,
    templates:     templates
}) }}

{% endblock %}
